// Captital int
Int x = 0;

// No expression
int y = ;

// While statement without condition
while () {

}

// No preceding if statement
else {

}

// No closing of block
if (True) { int x = 0;

// No colon in ternary
int x = (True) ? (2);

// No parentheses in ternary
int y = True ? 1 : 2;